# --- Secrets & host-specific (NEVER commit) ---
.env
.env.*
!.env.example
*.local.yaml
config.local.yaml
config.yaml
Makefile.local
# Real compose carries host paths / mount names — ship docker-compose.example.yml instead.
docker-compose.yml
secrets/
*.pem
*.key

# --- Runtime data (the DB is an index; proofs live on a writable volume) ---
data/
proofs/
*.db
*.sqlite
*.sqlite3
*-wal
*-shm

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/

# --- Local tooling / inputs (not part of the product) ---
.claude/
cairn.zip
